此目录用于存放项目专用（私有）库。
PlatformIO会将它们编译成静态库，并链接到可执行文件中。

每个库的源代码都应当存放在独立目录下，路径格式为：`lib/你的库名称/[代码]`。

下面以示例库Foo和Bar为例展示目录结构：
```
├── lib
│   ├── Bar
│   │   ├── docs
│   │   ├── examples
│   │   ├── src
│   │   │   ├── Bar.c
│   │   │   └── Bar.h
│   │   └── library.json（可选，用于自定义编译配置等，相关文档：https://docs.platformio.org/page/librarymanager/config.html）
│   │
│   ├── Foo
│   │   ├── Foo.c
│   │   └── Foo.h
│   │
│   └── README （即本文档）
│
├── platformio.ini
└── src
    └── main.c
```

在`src/main.c`中调用Foo与Bar库的示例代码：
```c
#include <Foo.h>
#include <Bar.h>

int main (void)
{
  ...
}
```

PlatformIO库依赖查找工具会扫描项目源代码，自动识别所需依赖库。

关于PlatformIO库依赖查找工具的更多信息：
- https://docs.platformio.org/page/librarymanager/ldf.html